*******************************************************************************
硬件培训内容

2. 两个patch，怎么设计解析器？
3. sdware实现，参考vlan
4. 文档完善
5. 继续看书写代码

根据usb_dev.c中的udev过滤器来过滤得到需要的udev消息注册两个函数来取得mtd设备和
mtd分区。

*******************************************************************************
    编码类：
    (-) 搞一下Kbuild系统的实现
    (-) 能够拷贝空洞但不用0填充的cp; 多项式加减乘除;平衡符号;中缀变后缀;队列
    (-) 排序：基数排序,卡式排序,桶式排序

    知识类：
    (*) 散列表
    (-) 总结ARM中的伪指令，访存指令
    (-) 总结内核启动参数的传递，arm启动参数的tag结构的使用
    (-) 编码系统，内码，外码，utf8，gbk，字体等
    (-) 几个cntl函数fcntl, ioctl, shmctl, sysctl, semctl, console_ioctl等

    文档类：
    (-) find, sed, awk, split/csplit, join, grep, sort, tr, fmt, man
    (-) debian package: dpkg, apt-get, apt-cache, apt-config, apt-key, apt-mark

*******************************************************************************
RFID Reader
    文档阅读：
        PN512，PN531数据手册，设计指南
        NFC标准，ISO14443标准
*******************************************************************************
    ucboot
        jffs2文件系统
	    +-------------+
	    | boot loader |
	    +-------------+
	    |             | <- u_kernel & u_rootfs & configs，只读
	    |    jffs2    | <- firmware update...，只读
	    |             | <- /boot, can be mounted，只读
	    +-------------+
	    |             |
	    |   unused    |
	    |             |
	    +-------------+
    (0) 文档（命令，终端，环境，闪存，malloc等问题）mtd和jffs2给用户态的接口。
    (0) flash保护，各个操作过程。
    (0) point unpoint
    (0) UBI和UBIFS什么关系

    (0) 模块化：整理ucboot内的fs，把它们模块化（至少ext2/vfat, jffs2, 最好支
    (0) vsc7501/openocd/linux增加mtd驱动patch

    (2) 增加mtdram功能，开发期不必烧写flash
    	持yaffs2（内核不支持，最于大page的NAND比较好）, ubifs，并调查cramfs）
    (4) 能否通过udev获得mtd name，通过udev monitor截取udev事件
    (5) 能否支持ubifs，如何实现，它可以支持断电保护功能。
    (6) monitor protect, env embedded, flash protected, env overwrite???
    (7) 添加CUSTOM_STOP_KEY逻辑；串口kermit下载功能；命令自动完成

#   flash_base->bd_info
#   (*) ucboot中分析grocx_flash和cfi_flash，尽量使兼容，然后在上面跑mtd device
#   (*) linux中跑mtdram，在上面跑jffs2，用mkfs.jffs2做image
#   (*) 编译内核，非模块方式和模块方式
#   (*) mtd设备如何分区，成功运行。
